# Windows GPU Setup Summary (Template)

This document is a placeholder. Host-specific setup notes should live in your
plugin repo (for example: `~/Code/hafs_scawful/docs/`).

See the actual host notes in `~/Code/hafs_scawful/docs/MEDICAL_MECHANICA_SETUP_SUMMARY.md`.

## Suggested Contents

- Host specs (GPU, RAM, OS)
- Ollama endpoints and installed models
- SSH users and keys
- Mount points and paths
- Validation checklist

See `docs/windows/WINDOWS_SETUP.md` for the generic setup guide.
